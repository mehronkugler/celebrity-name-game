require 'rails_helper'

RSpec.describe NamegameController, type: :controller do
  it "should keep track of player score"
  it "should keep track of how many rounds the player has finished"
  it "should create new cookies if they don't exist already"
  it "should set rounds to 1 if cookie doesn't exist"
end
